# The Cloudflare Worker serves the API and website from the same origin.
VITE_BACKEND=cloudflare
VITE_BASE_PATH=/

# Administrator and Cloudflare credentials belong only in encrypted deployment
# secrets. Never add them to this file.
